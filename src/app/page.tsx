'use client';

import { useEffect, useMemo, useState } from 'react';
import type { ChangeEvent, FormEvent, ReactNode } from 'react';

type Exercise = { name: string; equipment: string; sets: number; reps: string; muscle: string };
type Day = { dow: string; long: string; type: string; focus: string; ex: Exercise[] };
type AppData = { name: string; theme: 'light' | 'dark' | 'system'; profilePhoto: string; habits: Record<string, boolean>; sets: Record<string, number>; sleepHours: number; protein: number; water: number };
type Weather = { temperature: number; apparent: number; wind: number; code: number; fetchedAt: number };
type WeatherStatus = 'idle' | 'locating' | 'loading' | 'ready' | 'offline' | 'denied' | 'error';

type IconName = 'home' | 'workout' | 'habits' | 'stats' | 'profile' | 'clock' | 'trophy' | 'flame' | 'cloud' | 'moon' | 'sun' | 'download' | 'upload' | 'check' | 'spark';

const PROGRAM: Day[] = [
  { dow: 'MON', long: 'Monday', type: 'Chest + Shoulders', focus: 'Chest · Delts · Triceps', ex: [
    ['Incline Dumbbell Press', 'Dumbbell', 3, '6–10', 'Chest'], ['Dumbbell Shoulder Press', 'Dumbbell', 3, '8–12', 'Shoulders'], ['Cable Fly', 'Cable', 3, '12–15', 'Chest'], ['Cable Lateral Raise', 'Cable', 4, '12–20', 'Shoulders'], ['Triceps Rope Pushdown', 'Cable', 3, '10–15', 'Triceps'], ['Overhead Cable Triceps Extension', 'Cable', 3, '10–15', 'Triceps'], ['Incline Treadmill Walk', 'Treadmill', 1, '15–20 min', 'Cardio'],
  ].map(toExercise) },
  { dow: 'TUE', long: 'Tuesday', type: 'V-Taper + Back', focus: 'Lats · Back · Rear Delts · Arms', ex: [
    ['Pull-Ups', 'Bodyweight', 3, '5–10', 'Back'], ['Lat Pulldown', 'Cable', 3, '8–12', 'Back'], ['Chest-Supported Row', 'Machine', 3, '8–12', 'Back'], ['Single-Arm Cable Pulldown', 'Cable', 2, '10–15', 'Lats'], ['Reverse Pec Deck', 'Machine', 3, '12–15', 'Shoulders'], ['Dumbbell Curl', 'Dumbbell', 3, '10–15', 'Biceps'], ['Hammer Curl', 'Dumbbell', 2, '10–15', 'Biceps'], ['Incline Treadmill Walk', 'Treadmill', 1, '15–20 min', 'Cardio'],
  ].map(toExercise) },
  { dow: 'WED', long: 'Wednesday', type: 'Legs', focus: 'Quads · Hamstrings · Calves', ex: [
    ['Barbell Squat', 'Barbell', 3, '6–10', 'Legs'], ['Glute-Ham Raise', 'Bodyweight', 3, '8–12', 'Hamstrings'], ['Dumbbell Lunge', 'Dumbbell', 3, '10–15', 'Legs'], ['Lying Leg Curl', 'Machine', 3, '10–15', 'Hamstrings'], ['Standing Calf Raise', 'Machine', 3, '10–15', 'Calves'], ['Easy Treadmill Walk', 'Treadmill', 1, '10–15 min', 'Cardio'],
  ].map(toExercise) },
  { dow: 'THU', long: 'Thursday', type: 'V-Taper + Upper Body', focus: 'Chest · Back · Delts · Abs', ex: [
    ['Incline Machine Press', 'Machine', 3, '8–12', 'Chest'], ['Pull-Ups', 'Bodyweight', 3, '5–10', 'Back'], ['Chest-Supported Row', 'Machine', 3, '8–12', 'Back'], ['Machine Lateral Raise', 'Machine', 4, '12–20', 'Shoulders'], ['Straight-Arm Cable Pulldown', 'Cable', 3, '10–15', 'Lats'], ['Cable Fly', 'Cable', 3, '12–15', 'Chest'], ['Cable Crunch', 'Cable', 3, '10–15', 'Core'], ['Incline Treadmill Walk', 'Treadmill', 1, '15–20 min', 'Cardio'],
  ].map(toExercise) },
  { dow: 'FRI', long: 'Friday', type: 'Off', focus: 'Recovery · Mobility · Sleep', ex: [] },
  { dow: 'SAT', long: 'Saturday', type: 'Cardio + Core', focus: 'Conditioning · Core', ex: [
    ['Incline Treadmill Walk', 'Treadmill', 1, '30–40 min', 'Cardio'], ['Hanging Knee Raise', 'Bodyweight', 3, '10–15', 'Core'], ['Ab Wheel', 'Bodyweight', 3, '6–12', 'Core'],
  ].map(toExercise) },
  { dow: 'SUN', long: 'Sunday', type: 'Lower Body + Abs', focus: 'Quads · Posterior Chain · Abs', ex: [
    ['Leg Press', 'Machine', 3, '8–12', 'Legs'], ['Romanian Deadlift', 'Barbell', 3, '8–10', 'Hamstrings'], ['Leg Extension', 'Machine', 3, '12–15', 'Quads'], ['Seated Calf Raise', 'Machine', 4, '12–20', 'Calves'], ['Reverse Crunch', 'Bodyweight', 3, '10–15', 'Core'], ['Cable Crunch', 'Cable', 3, '10–15', 'Core'], ['Easy Treadmill Walk', 'Treadmill', 1, '10–15 min', 'Cardio'],
  ].map(toExercise) },
];

const HABITS = ['8 Hours Sleep', 'Protein Target', 'Workout Completed', 'Exercise Duration', 'Water Intake', 'Daily Movement', 'Recovery'];
const QUOTES = ['Consistency compounds quietly.', 'Do the next useful thing.', 'Strong habits make strong days.', 'Train with intention, recover with respect.', 'Small actions. Serious momentum.', 'You do not need perfect. You need repeatable.'];
const NAV = [['home', 'Home', 'home'], ['workout', 'Workout', 'workout'], ['habits', 'Habits', 'habits'], ['stats', 'Stats', 'stats'], ['profile', 'Profile', 'profile']] as const;
const KEY = 'pulseboard:v1';
const AUTH = 'pulseboard:session';
const WEATHER_KEY = 'pulseboard:weather:v1';

function toExercise(x: (string | number)[]): Exercise { return { name: String(x[0]), equipment: String(x[1]), sets: Number(x[2]), reps: String(x[3]), muscle: String(x[4]) }; }
function pad(n: number) { return String(n).padStart(2, '0'); }
function dateKey(d = new Date()) { return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`; }
function dayIndexForISO(iso: string) { const [year, month, day] = iso.split('-').map(Number); return (new Date(year, month - 1, day).getDay() + 6) % 7; }
function initial(): AppData { return { name: 'Hasan', theme: 'system', profilePhoto: '', habits: {}, sets: {}, sleepHours: 0, protein: 0, water: 0 }; }
function weatherLabel(code: number) { if (code === 0) return 'Clear sky'; if ([1, 2, 3].includes(code)) return 'Partly cloudy'; if ([45, 48].includes(code)) return 'Foggy'; if ([51, 53, 55, 56, 57].includes(code)) return 'Drizzle'; if ([61, 63, 65, 66, 67, 80, 81, 82].includes(code)) return 'Rain'; if ([71, 73, 75, 77, 85, 86].includes(code)) return 'Snow'; if ([95, 96, 99].includes(code)) return 'Thunderstorm'; return 'Local conditions'; }
function weatherIcon(code: number) { if (code === 0) return '☀'; if ([1, 2, 3].includes(code)) return '◌'; if ([45, 48].includes(code)) return '≋'; if ([51, 53, 55, 56, 57, 61, 63, 65, 66, 67, 80, 81, 82].includes(code)) return '☂'; if ([71, 73, 75, 77, 85, 86].includes(code)) return '✳'; return '⁙'; }
function progressFor(data: AppData, iso: string, index = dayIndexForISO(iso)) { const day = PROGRAM[index]; if (!day.ex.length) return 0; const complete = day.ex.filter((exercise, i) => (data.sets[`${iso}-${index}-${i}`] || 0) >= exercise.sets).length; return Math.round((complete / day.ex.length) * 100); }
function completedDates(data: AppData) { const dates = new Set(Object.keys(data.sets).map(key => key.slice(0, 10))); return [...dates].filter(iso => progressFor(data, iso) >= 100).sort().reverse(); }
function getStreak(data: AppData, today: Date) { let cursor = new Date(today.getFullYear(), today.getMonth(), today.getDate()); let streak = 0; if (progressFor(data, dateKey(cursor)) < 100) cursor.setDate(cursor.getDate() - 1); for (let i = 0; i < 370; i += 1) { const iso = dateKey(cursor); const index = dayIndexForISO(iso); if (!PROGRAM[index].ex.length) { cursor.setDate(cursor.getDate() - 1); continue; } if (progressFor(data, iso, index) < 100) break; streak += 1; cursor.setDate(cursor.getDate() - 1); } return streak; }
function gamification(data: AppData, today: Date) { const dates = completedDates(data); const totalSets = Object.values(data.sets).reduce((total, value) => total + Math.max(0, value), 0); const xp = dates.length * 100 + totalSets * 10 + Object.values(data.habits).filter(Boolean).length * 20; const level = Math.floor(xp / 500) + 1; return { xp, level, levelProgress: (xp % 500) / 500, streak: getStreak(data, today), workouts: dates.length, totalSets, lastWorkout: dates[0] || '' }; }
function formatLastWorkout(iso: string) { if (!iso) return 'No completed workout yet'; const index = dayIndexForISO(iso); const date = new Date(`${iso}T12:00:00`); return `${date.toLocaleDateString([], { month: 'short', day: 'numeric' })} · ${PROGRAM[index].type}`; }
function formatSleep(hours: number) { return hours ? `${hours.toFixed(hours % 1 ? 1 : 0)}h` : '—'; }

function Icon({ name, size = 18 }: { name: IconName; size?: number }) {
  const common = { width: size, height: size, viewBox: '0 0 24 24', fill: 'none', stroke: 'currentColor', strokeWidth: 1.8, strokeLinecap: 'round' as const, strokeLinejoin: 'round' as const, 'aria-hidden': true };
  const paths: Record<IconName, ReactNode> = {
    home: <><path d="m3 10 9-7 9 7" /><path d="M5 9v11h14V9M9 20v-6h6v6" /></>,
    workout: <><path d="M6 8v8M18 8v8M3 10v4M21 10v4M6 12h12" /></>,
    habits: <><circle cx="12" cy="12" r="8.5" /><path d="m8 12 2.5 2.5L16.5 9" /></>,
    stats: <><path d="M4 19V9M10 19V5M16 19v-7M22 19H2" /></>,
    profile: <><circle cx="12" cy="8" r="3.5" /><path d="M5 20c.8-3.2 3.2-5 7-5s6.2 1.8 7 5" /></>,
    clock: <><circle cx="12" cy="12" r="8.5" /><path d="M12 7v5l3 2" /></>,
    trophy: <><path d="M8 4h8v4a4 4 0 0 1-8 0V4ZM12 12v5M8 20h8M9 17h6" /><path d="M8 6H5v2a3 3 0 0 0 3 3M16 6h3v2a3 3 0 0 1-3 3" /></>,
    flame: <><path d="M12.3 21c4.2-.2 6.2-3 5.4-6.3-.5-2-2-3.2-3.4-4.6.1 2-1 3.2-2.3 3.7.2-3.1-1.5-5.2-3.2-7.8-.4 3.2-3.1 4.8-3.1 8.5A6.5 6.5 0 0 0 12.3 21Z" /></>,
    cloud: <><path d="M7 18h10a4 4 0 0 0 .7-7.9A6 6 0 0 0 6.3 9.5 4.3 4.3 0 0 0 7 18Z" /></>,
    moon: <path d="M20 15.5A8 8 0 0 1 8.5 4 8.5 8.5 0 1 0 20 15.5Z" />,
    sun: <><circle cx="12" cy="12" r="3.5" /><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4" /></>,
    download: <><path d="M12 3v12M7 10l5 5 5-5M4 20h16" /></>,
    upload: <><path d="M12 16V4M7 9l5-5 5 5M4 20h16" /></>,
    check: <path d="m5 12 4 4L19 6" />,
    spark: <><path d="m12 3 1.4 5.6L19 10l-5.6 1.4L12 17l-1.4-5.6L5 10l5.6-1.4L12 3Z" /><path d="m19 16 .5 2 2 .5-2 .5-.5 2-.5-2-2-.5 2-.5.5-2Z" /></>,
  };
  return <svg {...common}>{paths[name]}</svg>;
}

function ProgressRing({ value, label }: { value: number; label: string }) {
  const radius = 42; const circumference = 2 * Math.PI * radius;
  return <div className="progress-ring-wrap"><svg className="progress-ring" viewBox="0 0 100 100" aria-label={`${value}% ${label}`}><circle className="ring-track" cx="50" cy="50" r={radius} /><circle className="ring-value" cx="50" cy="50" r={radius} style={{ strokeDasharray: circumference, strokeDashoffset: circumference - (circumference * value) / 100 }} /></svg><div className="ring-copy"><strong>{value}%</strong><span>{label}</span></div></div>;
}

async function optimizeImage(file: File) {
  const source = await createImageBitmap(file); const max = 420; const scale = Math.min(1, max / Math.max(source.width, source.height)); const canvas = document.createElement('canvas'); canvas.width = Math.round(source.width * scale); canvas.height = Math.round(source.height * scale); const context = canvas.getContext('2d'); if (!context) throw new Error('Canvas unavailable'); context.drawImage(source, 0, 0, canvas.width, canvas.height); return canvas.toDataURL('image/jpeg', 0.78);
}

export default function Page() {
  const [ready, setReady] = useState(false); const [authed, setAuthed] = useState(false); const [password, setPassword] = useState(''); const [bad, setBad] = useState(false); const [data, setData] = useState<AppData>(initial); const [day, setDay] = useState((new Date().getDay() + 6) % 7); const [tab, setTab] = useState('home'); const [now, setNow] = useState(new Date()); const [offline, setOffline] = useState(false); const [toast, setToast] = useState(''); const [weather, setWeather] = useState<Weather | null>(null); const [weatherStatus, setWeatherStatus] = useState<WeatherStatus>('idle');
  useEffect(() => { try { const saved = localStorage.getItem(KEY); if (saved) { const parsed = JSON.parse(saved) as Partial<AppData> & { sleep?: number }; const next = { ...initial(), ...parsed }; if (typeof parsed.sleepHours !== 'number' && typeof parsed.sleep === 'number') next.sleepHours = parsed.sleep > 24 ? parsed.sleep / 60 : parsed.sleep; setData(next); } const cached = localStorage.getItem(WEATHER_KEY); if (cached) setWeather(JSON.parse(cached)); setAuthed(sessionStorage.getItem(AUTH) === 'ok'); } catch { /* recover with defaults */ } setOffline(!navigator.onLine); setReady(true); const timer = setInterval(() => setNow(new Date()), 1000); const online = () => { setOffline(false); setWeatherStatus(status => status === 'offline' ? 'idle' : status); }; const off = () => { setOffline(true); setWeatherStatus('offline'); }; addEventListener('online', online); addEventListener('offline', off); if ('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js').catch(() => undefined); return () => { clearInterval(timer); removeEventListener('online', online); removeEventListener('offline', off); }; }, []);
  useEffect(() => { if (ready) try { localStorage.setItem(KEY, JSON.stringify(data)); } catch { /* storage can be unavailable */ } }, [data, ready]);
  const selected = PROGRAM[day]; const key = dateKey(now); const todayIndex = (now.getDay() + 6) % 7; const selectedProgress = progressFor(data, key, day); const todayProgress = progressFor(data, key, todayIndex); const quote = QUOTES[Math.abs(Math.floor(new Date(`${key}T12:00:00`).getTime() / 86400000)) % QUOTES.length]; const game = useMemo(() => gamification(data, now), [data, now]);
  const update = (patch: Partial<AppData>) => setData(current => ({ ...current, ...patch }));
  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 1900); };
  function unlock(event: FormEvent) { event.preventDefault(); if (password === 'coco1513') { sessionStorage.setItem(AUTH, 'ok'); setAuthed(true); } else { setBad(true); window.setTimeout(() => setBad(false), 500); } }
  async function loadWeather() { if (!navigator.onLine) { setWeatherStatus('offline'); notify('Offline mode: showing the last saved conditions'); return; } if (!navigator.geolocation) { setWeatherStatus('denied'); notify('Location is not available in this browser'); return; } setWeatherStatus('locating'); navigator.geolocation.getCurrentPosition(async position => { try { setWeatherStatus('loading'); const { latitude, longitude } = position.coords; const response = await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,apparent_temperature,weather_code,wind_speed_10m&temperature_unit=fahrenheit&wind_speed_unit=mph&timezone=auto`); if (!response.ok) throw new Error('Weather request failed'); const result = await response.json(); const next: Weather = { temperature: Math.round(result.current.temperature_2m), apparent: Math.round(result.current.apparent_temperature), wind: Math.round(result.current.wind_speed_10m), code: result.current.weather_code, fetchedAt: Date.now() }; setWeather(next); setWeatherStatus('ready'); try { localStorage.setItem(WEATHER_KEY, JSON.stringify(next)); } catch { /* cached weather is optional */ } notify('Local weather updated'); } catch { setWeatherStatus(weather ? 'ready' : 'error'); notify(weather ? 'Showing saved weather' : 'Weather could not be loaded'); } }, () => { setWeatherStatus('denied'); notify('Allow location access to load local weather'); }, { enableHighAccuracy: false, timeout: 10000, maximumAge: 900000 }); }
  function toggleSet(index: number, set: number) { const id = `${key}-${day}-${index}`; const current = data.sets[id] || 0; update({ sets: { ...data.sets, [id]: current === set ? set - 1 : set } }); notify('Workout saved locally'); }
  function toggleHabit(habit: string) { const id = `${key}-${habit}`; update({ habits: { ...data.habits, [id]: !data.habits[id] } }); }
  function exportData() { const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const anchor = document.createElement('a'); anchor.href = url; anchor.download = 'pulseboard-backup.json'; anchor.click(); URL.revokeObjectURL(url); notify('Backup exported'); }
  async function handlePhoto(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; try { update({ profilePhoto: await optimizeImage(file) }); notify('Profile photo saved locally'); } catch { notify('That image could not be saved'); } }
  if (!ready) return null;
  if (!authed) return <div className="drawer"><form className={`modal ${bad ? 'shake' : ''}`} onSubmit={unlock}><div className="brand"><i className="brand-dot" />PULSEBOARD</div><h2>Private training space.</h2><p className="muted">Your dashboard is local, private, and ready offline.</p><div className="field"><label htmlFor="password">Access code</label><input autoFocus id="password" type="password" value={password} onChange={event => setPassword(event.target.value)} placeholder="Enter password" aria-invalid={bad} /></div>{bad && <p className="error-copy">That code did not work. Try again.</p>}<button className="button primary-button" style={{ width: '100%', marginTop: 10 }}>Unlock dashboard</button><p className="mono muted" style={{ marginTop: 18 }}>Client-side personal access gate</p></form></div>;
  return <div className="app"><div className="shell"><header className="topbar"><div className="brand"><i className="brand-dot" />PULSEBOARD <span className="brand-mark">/ OS</span></div><div className={`online mono ${offline ? 'offline' : ''}`}><i />{offline ? 'OFFLINE' : 'LOCAL MODE'}</div></header><main className="view" key={tab}>
    {tab === 'home' && <><section className="hero"><div><div className="eyebrow">{selected.long.toUpperCase()} · {key}</div><h1>GOOD {now.getHours() < 12 ? 'MORNING' : now.getHours() < 18 ? 'AFTERNOON' : 'EVENING'}, {data.name.toUpperCase()}.</h1><p>{quote} <em>Today is for {selected.type.toLowerCase()}.</em></p></div><div className="time-block"><div className="time-label"><Icon name="clock" size={12} /> LOCAL TIME <span className="live-dot" /> LIVE</div><div className="clock">{now.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit', second: '2-digit', hour12: true })}</div><div className="date">{now.toLocaleDateString([], { weekday: 'long', month: 'long', day: 'numeric' })}</div></div></section><div className="dashboard-grid"><section className="card signal-card"><div className="section-head"><div className="heading-with-icon"><Icon name="spark" /><h2>Today&apos;s signal</h2></div><span className="mono muted">{todayProgress}% ready</span></div><div className="signal-layout"><ProgressRing value={todayProgress} label="today" /><div className="metrics"><div className="metric"><Icon name="moon" size={15} /><strong>{formatSleep(data.sleepHours)}</strong><span>Sleep hours</span></div><div className="metric"><Icon name="spark" size={15} /><strong>{data.protein}g</strong><span>Protein target</span></div><div className="metric"><Icon name="cloud" size={15} /><strong>{data.water}/8</strong><span>Water glasses</span></div></div></div><div className="bar"><i style={{ width: `${todayProgress}%` }} /></div><p className="mono muted">{selected.ex.length ? `${PROGRAM[todayIndex].ex.filter((exercise, i) => (data.sets[`${key}-${todayIndex}-${i}`] || 0) >= exercise.sets).length}/${PROGRAM[todayIndex].ex.length} exercises complete` : 'Recovery day · protect your energy'}</p></section><section className="card weather-card"><div className="section-head"><div className="heading-with-icon"><Icon name="cloud" /><h2>Weather window</h2></div><span className="mono muted">{weatherStatus === 'ready' ? 'UPDATED' : weather ? 'CACHED' : 'LOCAL'}</span></div>{weather ? <div className="weather-content"><div className="weather-icon" aria-hidden="true">{weatherIcon(weather.code)}</div><div><strong className="weather-temp">{weather.temperature}°</strong><div className="weather-name">{weatherLabel(weather.code)}</div><div className="weather-meta">Feels {weather.apparent}° · Wind {weather.wind} mph</div></div></div> : <div className="weather-empty"><div className="weather-icon" aria-hidden="true">☼</div><div><b>Know your training window.</b><p className="small muted">Allow location once to see local conditions. The last result stays available offline.</p></div></div>}<button className="button secondary weather-button" onClick={loadWeather} disabled={weatherStatus === 'locating' || weatherStatus === 'loading'}>{weatherStatus === 'locating' ? 'Finding location…' : weatherStatus === 'loading' ? 'Updating weather…' : weather ? 'Refresh local weather' : 'Enable local weather'}</button></section><section className="card momentum-card"><div className="section-head"><div className="heading-with-icon"><Icon name="flame" /><h2>Momentum</h2></div><span className="level-badge">LVL {game.level}</span></div><div className="momentum-main"><div><strong className="level-number">{game.level}</strong><span className="mono muted">LEVEL</span></div><div className="streak-display"><Icon name="flame" size={25} /><strong>{game.streak}</strong><span>day streak</span></div></div><div className="level-track"><i style={{ width: `${Math.round(game.levelProgress * 100)}%` }} /></div><div className="level-meta"><span>{game.xp % 500} / 500 XP</span><span>{game.workouts} workouts</span></div></section><section className="card last-card"><div className="section-head"><div className="heading-with-icon"><Icon name="clock" /><h2>Last workout</h2></div><button className="text-button" onClick={() => setTab('workout')}>Open <span>→</span></button></div><div className="last-workout"><div className="last-icon"><Icon name="check" size={22} /></div><div><strong>{formatLastWorkout(game.lastWorkout)}</strong><p className="small muted">{game.lastWorkout ? 'Completed and stored on this device' : 'Your first session is waiting'}</p></div></div></section></div></>}
    {tab === 'workout' && <><div className="hero"><div><div className="eyebrow">Weekly training journal · {key}</div><h1>{selected.type.toUpperCase()}</h1><p>{selected.focus}. Log each set and build your record.</p></div><ProgressRing value={selectedProgress} label="session" /></div><div className="tabs">{PROGRAM.map((item, index) => <button key={item.dow} className={`tab ${day === index ? 'active' : ''}`} onClick={() => setDay(index)}><span>{item.dow}</span><small>{item.ex.length ? `${progressFor(data, key, index)}%` : 'OFF'}</small></button>)}</div><section className="card"><div className="section-head"><div><div className="eyebrow">{selected.long}</div><h2>{selected.focus}</h2></div><span className="mono muted">{selectedProgress}%</span></div>{selected.ex.length ? <div className="workout-list">{selected.ex.map((exercise, index) => { const got = data.sets[`${key}-${day}-${index}`] || 0; const done = got >= exercise.sets; return <article className={`exercise ${done ? 'done' : ''}`} key={`${exercise.name}-${index}`}><span className="number">{String(index + 1).padStart(2, '0')}</span><div><div className="exercise-name">{exercise.name}</div><div className="exercise-meta">{exercise.muscle} · {exercise.equipment} · {exercise.sets} × {exercise.reps}</div></div><div className="sets">{Array.from({ length: exercise.sets }, (_, set) => <button aria-label={`Set ${set + 1}`} className={`set ${set < got ? 'on' : ''}`} onClick={() => toggleSet(index, set + 1)} key={set}>{set + 1}</button>)}</div></article>; })}</div> : <div className="recovery"><Icon name="moon" size={38} /><div className="eyebrow">Recovery protocol</div><h2>OFF DAY</h2><p className="muted">Sleep long, hydrate, walk lightly, and return stronger tomorrow.</p></div>}</section></>}
    {tab === 'habits' && <><div className="hero"><div><div className="eyebrow">Daily systems · {key}</div><h1>HABITS.</h1><p>One authoritative checklist for the small actions that compound.</p></div><div className="hero-stat"><strong>{Object.values(data.habits).filter(Boolean).length}</strong><span>of {HABITS.length} done</span></div></div><section className="card"><div className="section-head"><div className="heading-with-icon"><Icon name="habits" /><h2>Today&apos;s habits</h2></div><span className="mono muted">{Object.values(data.habits).filter(Boolean).length}/{HABITS.length}</span></div><div className="habit-list">{HABITS.map(habit => { const id = `${key}-${habit}`; const on = !!data.habits[id]; return <div className="habit" key={habit}><div className="habit-row"><div><b>{habit}</b><div className="small muted">{habit === 'Sleep Schedule' ? 'Aim for a consistent window' : 'Mark complete when the action is done'}</div></div><button className={`check ${on ? 'on' : ''}`} aria-label={`Mark ${habit} complete`} onClick={() => toggleHabit(habit)}>{on ? <Icon name="check" size={17} /> : ''}</button></div></div>; })}</div></section></>}
    {tab === 'stats' && <><div className="hero"><div><div className="eyebrow">Consistency report</div><h1>STATS.</h1><p>Your progress is calculated from local workout, habit, and recovery records.</p></div><ProgressRing value={Math.round(game.levelProgress * 100)} label="to next level" /></div><div className="stats-layout"><section className="card"><div className="section-head"><div className="heading-with-icon"><Icon name="trophy" /><h2>Achievements</h2></div><span className="mono muted">{[game.workouts >= 1, game.workouts >= 3, game.streak >= 7, game.totalSets >= 100, Object.values(data.habits).filter(Boolean).length >= 10].filter(Boolean).length}/5</span></div><div className="achievement-grid">{[[game.workouts >= 1, 'First session', 'Complete a full workout', 'workout'], [game.workouts >= 3, 'Showing up', 'Complete 3 workouts', 'flame'], [game.streak >= 7, 'Unbroken', 'Build a 7 day streak', 'spark'], [game.totalSets >= 100, 'Volume mode', 'Log 100 sets', 'stats'], [Object.values(data.habits).filter(Boolean).length >= 10, 'Daily systems', 'Complete 10 habits', 'habits']].map(([unlocked, title, copy, icon]) => <div className={`achievement ${unlocked ? 'unlocked' : ''}`} key={title as string}><div className="achievement-icon"><Icon name={icon as IconName} size={20} /></div><div><b>{title as string}</b><span>{copy as string}</span></div><i>{unlocked ? <Icon name="check" size={14} /> : '—'}</i></div>)}</div></section><section className="card"><div className="section-head"><div className="heading-with-icon"><Icon name="stats" /><h2>Training record</h2></div><span className="level-badge">LVL {game.level}</span></div><div className="stat-grid"><div className="stat"><b>{game.workouts}</b><span className="mono muted">workouts</span></div><div className="stat"><b>{game.totalSets}</b><span className="mono muted">sets logged</span></div><div className="stat"><b>{game.streak}</b><span className="mono muted">day streak</span></div></div><div className="record-progress"><div><span>Level progress</span><b>{game.xp % 500} / 500 XP</b></div><div className="bar"><i style={{ width: `${Math.round(game.levelProgress * 100)}%` }} /></div></div><div className="section-head record-heading"><h2>Weekly program</h2><span className="mono muted">7-day view</span></div>{PROGRAM.map((item, index) => <div className="week-record" key={item.dow}><b>{item.dow} · {item.type}</b><span className="mono muted">{item.ex.length ? `${progressFor(data, key, index)}%` : 'REST'}</span></div>)}</section></div></>}
    {tab === 'profile' && <><div className="hero"><div><div className="eyebrow">Personal settings</div><h1>PROFILE.</h1><p>Make this system yours. Everything stays on this device.</p></div>{data.profilePhoto ? <img className="hero-avatar" src={data.profilePhoto} alt="Profile" /> : <div className="hero-avatar avatar-placeholder"><Icon name="profile" size={38} /></div>}</div><div className="profile-layout"><section className="card"><div className="section-head"><div className="heading-with-icon"><Icon name="profile" /><h2>Identity</h2></div><span className="mono muted">LOCAL ONLY</span></div><div className="profile-editor"><div className="profile-photo-wrap">{data.profilePhoto ? <img src={data.profilePhoto} alt="Your profile" className="profile-photo" /> : <div className="profile-photo avatar-placeholder"><Icon name="profile" size={42} /></div>}<label className="photo-button" htmlFor="profile-photo"><Icon name="upload" size={15} /> {data.profilePhoto ? 'Replace photo' : 'Add photo'}</label><input id="profile-photo" type="file" accept="image/*" hidden onChange={handlePhoto} />{data.profilePhoto && <button className="text-button remove-photo" onClick={() => update({ profilePhoto: '' })}>Remove</button>}</div><div className="form"><div className="field"><label htmlFor="name">Your name</label><input id="name" value={data.name} onChange={event => update({ name: event.target.value })} /></div><div className="field"><label htmlFor="sleep">Sleep hours</label><input id="sleep" type="number" min="0" max="24" step="0.5" value={data.sleepHours || ''} placeholder="8" onChange={event => update({ sleepHours: Number(event.target.value) })} /></div><div className="field"><label htmlFor="water">Water glasses · target 8</label><input id="water" type="number" min="0" max="20" value={data.water} onChange={event => update({ water: Number(event.target.value) })} /></div></div></div></section><section className="card"><div className="section-head"><div className="heading-with-icon"><Icon name="download" /><h2>Your data</h2></div></div><p className="muted small">Your profile photo is resized before local storage. Export a backup whenever you want.</p><div className="profile-actions"><button className="button primary-button" onClick={exportData}><Icon name="download" size={15} /> Export JSON</button><button className="button secondary" onClick={() => { if (confirm('Reset all local Pulseboard data?')) { localStorage.removeItem(KEY); setData(initial()); notify('Data reset'); } }}>Reset data</button></div><div className="home-screen-tip"><Icon name="spark" size={20} /><div><b>Add Pulseboard to Home Screen</b><p className="small muted">In Safari, tap Share → Add to Home Screen. The app shell and records remain available offline.</p></div></div></section></div></>}
  </main></div><nav className="nav" aria-label="Primary navigation">{NAV.map(([id, label, icon]) => <button className={tab === id ? 'active' : ''} onClick={() => setTab(id)} key={id}><Icon name={icon as IconName} size={18} /><span>{label}</span></button>)}</nav>{toast && <div className="toast">{toast}</div>}</div>;
}
